/**
 * AUTOGENERATED bundle of every Aether Forge JSON schema.
 * Source: src/aether_forge/schemas/{artifacts,common,runtime}/*.schema.json.
 * Regenerate via `bun run generate:schemas`. DO NOT EDIT BY HAND.
 * CI verifies the committed output matches a fresh regeneration.
 */

export type AgentSpec = ArtifactEnvelope & {
  artifactType?: "agent-spec";
  metadata: {
    name: string;
    summary: string;
    domain: string;
    owners?: string[];
    tags?: string[];
    status?: string;
    [k: string]: unknown;
  };
  objective: {
    primaryGoal: string;
    /**
     * @minItems 1
     */
    successMetrics: [
      {
        [k: string]: unknown;
      },
      ...{
        [k: string]: unknown;
      }[]
    ];
    nonGoals?: string[];
    constraintsSummary?: string[];
    failureModes?: string[];
    [k: string]: unknown;
  };
  environmentContract: {
    /**
     * @minItems 1
     */
    allowedEnvironments: [string, ...string[]];
    defaultEnvironment: string;
    environmentNotes?: string[];
    promotionPath?: string[];
    [k: string]: unknown;
  };
  /**
   * @minItems 1
   */
  capabilityRefs: [string | ArtifactRef, ...(string | ArtifactRef)[]];
  policyRefs: string[];
  evaluation: {
    scenarioPackRef: ArtifactRef;
    requiredOutcomes: string[];
    successThresholds: {
      [k: string]: unknown;
    };
    [k: string]: unknown;
  };
  promotion: {
    allowedTargets: string[];
    requiredApprovals: number;
    [k: string]: unknown;
  };
  [k: string]: unknown;
};
export type Compatibility = {
  [k: string]: unknown;
} & {
  status: "backward-compatible" | "forward-compatible" | "breaking" | "incompatible";
  previousArtifactVersion: string | null;
  migrationRef: ArtifactRef | null;
};
export type CapabilityManifest = ArtifactEnvelope & {
  artifactType?: "capability-manifest";
  credentialHandles: {
    handleId: string;
    kind: string;
    allowedEnvironments: string[];
    maximumAccessScope: {
      [k: string]: unknown;
    };
    rotationExpectation: string;
    expiresAt?: string;
    ttlPolicy?: {
      [k: string]: unknown;
    };
    [k: string]: unknown;
  }[];
  /**
   * @minItems 1
   */
  capabilities: [
    {
      [k: string]: unknown;
    },
    ...{
      [k: string]: unknown;
    }[]
  ];
  [k: string]: unknown;
};
export type MemoryRecord = ArtifactEnvelope & {
  artifactType?: "memory-record";
  ownerAgentId: string | null;
  artifactSetId?: string;
  memoryType:
    | "strategy-context"
    | "operator-preference"
    | "environment-fact"
    | "venue-fact"
    | "incident-learning"
    | "evaluation-finding"
    | "decision-history";
  scope: "session" | "agent" | "project" | "environment";
  environment: "local" | "sandbox" | "shadow" | "paper" | "canary-live" | "production";
  content: {
    [k: string]: unknown;
  };
  summary: string;
  source: string;
  confidence: number;
  sensitivity: "public" | "internal" | "sensitive" | "restricted";
  createdAt: string;
  updatedAt: string;
  expiresAt?: string | null;
  retentionPolicy?: string | null;
  provenanceRefs: string[];
  tags: string[];
  metadata?: {
    [k: string]: unknown;
  };
  [k: string]: unknown;
};
export type PolicyBundle = ArtifactEnvelope & {
  artifactType?: "policy-bundle";
  bundleName: string;
  defaultAction: "allow" | "deny" | "hold";
  rules: {
    maxNotionalUsd?: number;
    walletAllowedChains?: string[];
    maxWalletTransferAmount?: number;
    requireApprovalEnvironments?: string[];
    requireWalletApprovalEnvironments?: string[];
    enforceStalenessChecks?: boolean;
    [k: string]: unknown;
  };
  [k: string]: unknown;
};
export type PromotionRecord = ArtifactEnvelope & {
  artifactType?: "promotion-record";
  /**
   * @minItems 1
   */
  artifactRefs: [ArtifactRef, ...ArtifactRef[]];
  evaluationSummary: {
    [k: string]: unknown;
  };
  promotionDecision: {
    sourceEnvironment: string;
    targetEnvironment: string;
    decisionOutcome: "approved" | "approved-with-limits" | "held" | "rejected";
    approvers: string[];
    policyBundleVersion: string;
    scenarioPackVersion: string;
    rolloutLimits: {
      [k: string]: unknown;
    };
    [k: string]: unknown;
  };
  residualRisks: {
    [k: string]: unknown;
  }[];
  rolloutPlan: {
    [k: string]: unknown;
  };
  [k: string]: unknown;
};
export type ResearchRecord = ArtifactEnvelope & {
  artifactType?: "research-record";
  researchPlan: {
    [k: string]: unknown;
  };
  evidenceLog: {
    [k: string]: unknown;
  }[];
  findings: {
    [k: string]: unknown;
  }[];
  blockers: {
    [k: string]: unknown;
  }[];
  activeComparisonContract: ActiveComparisonContract;
  iterationLedger: {
    candidateId: string;
    parentCandidateId: string | null;
    hypothesis: string;
    changedSurfaces: string[];
    budgetConsumed: {
      [k: string]: unknown;
    };
    evaluationConditions: {
      [k: string]: unknown;
    };
    measuredOutcomes: {
      [k: string]: unknown;
    };
    decisionStatus: "keep" | "discard" | "blocked" | "execution-failure";
    decisionRationale: string;
    [k: string]: unknown;
  }[];
  stopRationale: {
    [k: string]: unknown;
  };
  [k: string]: unknown;
};
export type ScaffoldManifest = ArtifactEnvelope & {
  artifactType?: "scaffold-manifest";
  paths: {
    [k: string]: unknown;
  };
  /**
   * @minItems 1
   */
  ownershipZones: [
    {
      pathPattern: string;
      zoneType: "generated" | "user-owned" | "protected";
      regenerationMode: "safe-update" | "propose-patch" | "blocked";
    },
    ...{
      pathPattern: string;
      zoneType: "generated" | "user-owned" | "protected";
      regenerationMode: "safe-update" | "propose-patch" | "blocked";
    }[]
  ];
  regenerationModes: {
    "safe-update": string[];
    "propose-patch": string[];
    blocked: string[];
    [k: string]: unknown;
  };
  [k: string]: unknown;
};
export type ScenarioPack = ArtifactEnvelope & {
  artifactType?: "scenario-pack";
  /**
   * @minItems 1
   */
  scenarios: [
    {
      scenarioId: string;
      category: "baseline" | "edge" | "policy-violation" | "incident-regression" | "stress";
      environmentKind: string;
      inputs: {
        [k: string]: unknown;
      };
      expectedOutcome: {
        [k: string]: unknown;
      };
      blockingReasonIds: string[];
      metrics: {
        [k: string]: unknown;
      };
      replayClass: "exactly-replayable" | "approximately-replayable" | "audit-only";
      [k: string]: unknown;
    },
    ...{
      scenarioId: string;
      category: "baseline" | "edge" | "policy-violation" | "incident-regression" | "stress";
      environmentKind: string;
      inputs: {
        [k: string]: unknown;
      };
      expectedOutcome: {
        [k: string]: unknown;
      };
      blockingReasonIds: string[];
      metrics: {
        [k: string]: unknown;
      };
      replayClass: "exactly-replayable" | "approximately-replayable" | "audit-only";
      [k: string]: unknown;
    }[]
  ];
  thresholds: {
    [k: string]: unknown;
  };
  [k: string]: unknown;
};
/**
 * Declarative shape for configuring a signer that fulfills the DelegatedSigner protocol (`aether_forge.crypto.signers`). v0.22.0+ (Sprint 2.3 / FP-3). Used by hosted-marketplace patterns where the agent's own OWS wallet is NOT the EIP-3009 signer (e.g. a browser-side wallet via relay, an HSM-backed delegated secret, or a constrained session key on top of any of the above).
 */
export type AetherForgeDelegatedSignerDeclaration = {
  [k: string]: unknown;
};
/**
 * Pins the JSON shape every planner emits before the runtime translates it into typed StepProposal objects. v0.23.0+ (FP-1, cross-language). The companion `docs/specs/planner-output.md` is the normative behavioral spec for the parser (preamble stripping, balanced-brace recovery, retry semantics); this file is the structural contract any Python or TypeScript planner can validate against. Both the Python `aether_forge.planner._extract_json` recovery path and the TypeScript `@aether-forge/sdk` `parsePlannerOutput` produce values conforming to this schema.
 */
export type AetherForgePlannerOutput =
  | {
      steps: PlannerStep[];
      [k: string]: unknown;
    }
  | PlannerStep[];

export interface ArtifactEnvelope {
  artifactType: string;
  schemaVersion: string;
  artifactId: string;
  artifactVersion: string;
  artifactSetId: string;
  title: string;
  generator: {
    name: string;
    version: string;
    inputDigest: string;
    templateVersion?: string;
    normalizerVersion?: string;
    importerVersions?: {
      [k: string]: string;
    };
  };
  compatibility: Compatibility;
  provenance: {
    createdAt: string;
    sourceMode: "fast" | "slow" | "manual" | "imported";
    sourcePromptDigest?: string;
    importSource?: string;
    derivedFrom?: ArtifactRef[];
  };
  [k: string]: unknown;
}
export interface ArtifactRef {
  artifactType: string;
  artifactId: string;
  artifactVersion: string;
}
/**
 * Evidence-backed reputation snapshot written after each agent run, optionally extended with confidence, window, identity-binding, claims, and publication blocks (docs/specs/agent-reputation.md). The v0 runtime record validates as-is; extension blocks are additive.
 */
export interface ReputationRecord {
  kind: "aether-forge/reputation-record";
  version: string;
  artifactSetId?: string | null;
  agentName?: string | null;
  environment?: "sandbox" | "paper" | "canary-live" | "production" | null;
  snapshot: {
    score: number;
    tier: "strong" | "developing" | "weak";
    components: {
      [k: string]: {
        score: number;
        weight: number;
      };
    };
    unobserved: string[];
    /**
     * Raw counts the components were computed from, embedded for recomputability.
     */
    inputs: {
      [k: string]: unknown;
    };
    computedAt: string;
    [k: string]: unknown;
  };
  scorer: string;
  /**
   * v1 extension — evidence-volume confidence; tier labels are gated on this.
   */
  confidence?: {
    value: number;
    basis?: string;
  };
  /**
   * v1 extension — the contiguous evidence window the published aggregate covers.
   */
  window?: {
    from?: string;
    to?: string;
    runs?: number;
    contiguous?: boolean;
  };
  /**
   * v1 extension — binds the snapshot to the agent identity; required for any published record.
   */
  identity?: {
    capabilitiesHash: string;
    agentAddress?: string;
    attestationRef?: string;
    signature?: string;
  };
  /**
   * v1 extension — claimed-vs-verified performance triples. ZK-ready via method: zk-proof.
   */
  claims?: {
    metric: string;
    claimed: number;
    verified?: number | null;
    method: "replay-ledger" | "onchain" | "attestor-audit" | "zk-proof";
    proofRef?: string;
  }[];
  /**
   * v1 extension — where this record was/will be published.
   */
  publication?: {
    target: "local" | "erc8004" | "hosted-index";
    txRef?: string;
  };
  [k: string]: unknown;
}
export interface ActiveComparisonContract {
  comparisonId: string;
  evaluatorVersion: string;
  policyThresholdsDigest: string;
  scenarioPackRef: ArtifactRef;
  normalizationRules: {
    [k: string]: unknown;
  };
  allowedMutationSurfaces: {
    artifactType: string;
    pathPattern: string;
  }[];
  budgetRules: {
    [k: string]: unknown;
  };
  artifactSetId: string;
}
export interface MigrationContract {
  fromVersion: string;
  toVersion: string;
  transformSteps: string[];
  lossyFields: string[];
  validationChecks: string[];
  /**
   * Identifier resolved against `migrations.TransformRegistry` to look up the actual transform callable (v0.22.0+, FP-4). Without this, the migration contract is documentation-only — humans read the transformSteps and apply changes by hand. With it, `MigrationRunner` can execute the transform end-to-end.
   */
  transformRef?: string;
  /**
   * Execution policy for `MigrationRunner` (v0.22.0+).
   */
  policy?: {
    /**
     * When false (default), the runner refuses to apply a contract whose `lossyFields` is non-empty. When true, the caller has explicitly acknowledged that fields will be dropped — equivalent to a `_weakens_criteria` override in scope.
     */
    lossyOk?: boolean;
  };
}
/**
 * Per-agent runtime configuration baked at generation time by `forge generate-fast`. Read by every CLI command that resolves planner / runtime / memory settings. Permissive on unknown top-level keys so user-managed sections (e.g. `mcp_servers`, `adapters`) can extend the contract without breaking validation; strict on the v0.21.0+ provenance fields the framework writes itself.
 */
export interface AetherForgeAgentConfigAetherForgeJson {
  /**
   * Risk tier the agent will run under (v0.22.0+, FP-2 deepening). `local` (default) tolerates autodetected planners and heuristic fallback; `staging` requires explicit planner; `production` is strict — `forge doctor` fails when planner.source=autodetected or planner.mode=heuristic.
   */
  deploymentProfile?: "local" | "staging" | "production";
  planner?: {
    /**
     * Planner backend. `heuristic` is the safe fallback (no LLM); the cloud modes (`anthropic`/`openai`/`gemini`/`openrouter`) and `ollama` are LLM-driven; `function-call` and `openai-compatible` are pluggable provider shims; `static` reads a fixture file (test only).
     */
    mode?:
      | "heuristic"
      | "static"
      | "openai-compatible"
      | "function-call"
      | "anthropic"
      | "gemini"
      | "openai"
      | "openrouter"
      | "ollama";
    /**
     * Provider-specific model id.
     */
    model?: string;
    /**
     * Override the provider base URL.
     */
    baseUrl?: string;
    /**
     * Direct API key (prefer apiKeyEnv in committed configs).
     */
    apiKey?: string;
    /**
     * Name of the env var that holds the API key.
     */
    apiKeyEnv?: string;
    /**
     * Path to a JSON file for the static planner mode.
     */
    staticResponseFile?: string;
    /**
     * Provenance of the planner choice (v0.21.0+). `explicit` = operator passed `--planner-mode` or set `AETHER_FORGE_PLANNER_MODE`; `autodetected` = `cli._autodetect_planner` picked it.
     */
    source?: "explicit" | "autodetected";
    /**
     * ISO-8601 timestamp at which autodetect ran. Present only when source=autodetected.
     */
    detectedAt?: string;
    /**
     * When true, the planner uses provider-native tool-use (Anthropic tool_use / OpenAI function_calling) instead of string-parsed JSON. Sprint 2.1 / FP-1 deepening. Default false.
     */
    toolMode?: boolean;
    [k: string]: unknown;
  };
  runtime?: {
    cryptoRouter?: "mock" | "public-market-data" | "paper-trading" | "sim-wallet" | "ows-wallet" | "scaffold-live";
    [k: string]: unknown;
  };
  memory?: {
    [k: string]: unknown;
  };
  security?: {
    [k: string]: unknown;
  };
  market_data?: {
    [k: string]: unknown;
  };
  adapters?: {
    [k: string]: unknown;
  };
  mcp_servers?: {
    [k: string]: unknown;
  };
  [k: string]: unknown;
}
export interface PlannerStep {
  /**
   * One of the five StepKind values. Matches `aether_forge.runtime.StepKind` (Python) and `StepKind` (TypeScript).
   */
  kind: "reason" | "use-capability" | "request-approval" | "replan" | "report-gap";
  description: string;
  /**
   * Required when kind is `use-capability` or `request-approval`. The Python parser also accepts the snake_case alias `capability_id`.
   */
  capabilityId?: string;
  /**
   * Snake-case alias of `capabilityId` for back-compat with older planners. New planners should emit `capabilityId`.
   */
  capability_id?: string;
  /**
   * Step-specific arguments. For `use-capability` / `request-approval`, payload is forwarded to the capability handler. For `reason` steps, payload may carry `mark_complete: true` to advance session state.
   */
  payload?: {
    [k: string]: unknown;
  };
  [k: string]: unknown;
}
/**
 * Pins the contract between an Aether Forge capability-manifest and the provider-native tool-use definitions emitted by `adapters.function_call.build_tool_schema_from_manifest`. v0.22.0+ (FP-1 deepening). Used by `PromptDrivenPlanner(tool_mode=True)` to opt out of JSON-string parsing entirely. Each declared capability becomes one tool. The OpenAI shape is canonical (used directly with `OpenAICompatiblePlanningModel.complete_with_tools`); `to_anthropic_tool_schema` adapts it to Anthropic's `input_schema` shape on the way out.
 */
export interface AetherForgePlannerToolUseMapping {
  tools: OpenAITool[];
}
export interface OpenAITool {
  /**
   * OpenAI tool-use shape. The Anthropic adapter rewraps this to `{name, description, input_schema}`.
   */
  type: "function";
  function: {
    /**
     * The capability id (matches `capabilityManifest.capabilities[].capabilityId`).
     */
    name: string;
    /**
     * Capability description shown to the model; sourced from `description` or `name` on the manifest entry.
     */
    description: string;
    /**
     * JSON Schema for the tool's input. Sourced from `capabilities[].inputSchema` when present, otherwise an empty permissive object.
     */
    parameters: {
      [k: string]: unknown;
    };
  };
}
export interface PolicyDecisionRecord {
  decisionId: string;
  policyBundleVersion: string;
  inputScope: {
    [k: string]: unknown;
  };
  ruleMatches: string[];
  severity: "info" | "warning" | "error" | "critical";
  approvalPath: string[];
  finalDisposition: "allow" | "deny" | "hold";
}
export interface RuntimeStepLedgerEntry {
  stepId: string;
  stateSnapshotRef: string;
  plannedAction: {
    [k: string]: unknown;
  };
  policyDecisionRef: string;
  approvalOutcome: {
    [k: string]: unknown;
  };
  effectResult: {
    [k: string]: unknown;
  };
  postActionInvariantResult: {
    [k: string]: unknown;
  };
  resultingStateSnapshotRef: string;
}
