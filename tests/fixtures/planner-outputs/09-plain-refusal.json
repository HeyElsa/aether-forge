{
  "description": "Plain-text refusal with no recoverable JSON. Both implementations MUST raise PlannerParseError.",
  "input": "I cannot help with that request.",
  "expected": {
    "outcome": "parse-failure"
  }
}
